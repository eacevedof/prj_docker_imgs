# symf03
version: "3"

services:
  # dbmaria:
  #   build: ./mariadb/
  #   container_name: cmari03
  #   hostname: hmari03

  #   environment:
  #     MYSQL_ROOT_PASSWORD: 1234

  #   volumes:
  #     - ./db_volume/initdb:/docker-entrypoint-initdb.d/
  #     - ./db_volume/mysql:/var/lib/mysql
    
  #   ports:
  #     - "3306:3306"      
  #   networks:
  #     - net
  #   command:
  #     'mysqld --innodb-flush-method=fsync'

  php:
    build: ./php7_fpm/
    container_name: cphp03
    hostname: hphp03
    volumes:
      # - ./php7_fpm/entrypoint.sh:/var/www/html/entrypoint.sh
      # - ./php7_fpm/:/home/user/entrypoint
      - ./test/www:/var/www/html
    networks:
      - net
    entrypoint: 
      - /home/user/bash_scripts/entrypoint.sh
    
  # nginx:
  #   build: ./nginx/
  #   container_name: cngx03
  #   hostname: hngx03
  #   ports:
  #     - "2000:80"
  #   volumes:
  #     - ./www/html:/var/www/html
  #     - ./nginx/site.conf:/etc/nginx/conf.d/default.conf
  #   networks:
  #     - net

networks:
  net:
    driver: bridge
