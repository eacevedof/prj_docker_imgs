version: "3.5"

services:
  kafka:
    hostname: kafka
    container_name: cont-kafka
    build:
      context: docker/kafka
    command: tail -f /dev/null
    networks:
      - kafka-net
      - mariadb-univ_net

networks:
  kafka-net:
  mariadb-univ_net:
    external: true
